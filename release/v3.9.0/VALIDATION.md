# V3.9.0 发布验证

- Go 1.26.5：`go test ./...` 全部通过。
- `go vet ./...` 通过；前端 app.js 的 Node 语法检查通过。
- 原配置 v31 的隔离副本启动后迁移到 v33；迁移备份与迁移前文件逐字节一致。原项目 config 保持 v31，未用测试配置覆盖。
- 既有配置值保留；允许的迁移变化为版本号、SQL 插件别名归一化、缺失时间规则补齐、已知默认 OR 延迟条件修复以及敏感规则 validator 补齐。
- 使用本机预览程序打开持久配置页面，确认公共配置／插件配置分区、桌面端卡片单列、关键词双框和卡片原地展开／再次点击收起；展开和收起均保持滚动位置，保存入口在缺少密码时正确阻止提交。
- Linux amd64 / arm64：CGO_ENABLED=0，交叉编译成功并核对 ELF 文件头；未在 Linux 主机运行。
- 未执行 race 检查（当前环境缺少 C 编译工具链）。

## 升级

按服务器架构选择对应文件，停止旧进程、替换原可执行文件并恢复执行权限，然后使用原启动命令和工作目录重启。无需替换 config 或签名脚本。首次启动自动备份旧配置并迁移，配置目录须可写。

压缩包内可执行文件已统一命名为 jungle_happy_Scan；SHA256SUMS 包含独立二进制和压缩包校验值。详细变更和规则示例见仓库 docs/RELEASE-v3.9.0.md。

2026-09-14 前端分区更新：API 回归测试及 JS 语法检查通过；浏览器确认中文卡片、短信专属项显示、SQL 项隐藏、关键词双框、桌面端单列以及卡片展开／再次点击收起时滚动位置不变。重新编译并打包 Linux 双架构。
